package com.colegio.service.impl;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.colegio.entity.Usuario;
import com.colegio.repository.IUsuarioRepo;

//@Service
public class UsuarioServiceImpl {

	/*@Autowired
    private IUsuarioRepo usuarioRepo;
	
	public Usuario guardar(Usuario user) {
		return usuarioRepo.save(user);
	}
	*/
	/*public Integer update(Usuario user) {
		return usuarioRepo.update(user.getId(), user);
	}
	
	public String deleteById(Integer usuarioId) {
		return usuarioRepo.delete(usuarioId);
	}
	
	public Usuario getEmployeeById(Integer usuarioId) {
        return usuarioRepo.getEmployeeById(usuarioId);
    }*/
}
